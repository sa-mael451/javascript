let a = 56, b = 45.45 // number
let str = "Dev", text = 'wow' // string
let c = true // boolean
let d // undefined
let e = null // null
let big = 12345678901234567890n; //bigint
// one more 'symbol': these are 7 primitive datatypes in js

console.log (a, b, str, text, c, d, e, big);
console.log( typeof (a), typeof str, typeof (c), typeof (d), typeof (e), typeof (big));
